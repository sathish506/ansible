- name: Demo on loops
  hosts: all
  tasks: 
  - name: Demo on using loops
    ansible.builtin.debug:
      msg: "Value is {{item}}"           # Item is a key work to represent the value in the
    loop:
     - 100
     - 300
     - 500


